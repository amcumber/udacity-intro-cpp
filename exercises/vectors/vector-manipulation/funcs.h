#include <vector>

using namespace std;

vector<int> sloppy_subtract(vector<int> vec1, vector<int> vec2);
    /* Subtract vector 1 by vector 2 such: vec1 - vec2
     * Does not perform any error checking
     */

vector<int> sloppy_multiply(vector<int> vec1, vector<int> vec2);
    /* Multiply vector 1 with vector 2 such: vec1 * vec2
     * Does not perform any error checking
     */

void print_vector(vector<int> vec);
    /* Prints the contents of a vector */